describe("environment", () => {
  it("should have a browser object defined", () => {
    expect(browser).toBeDefined()
  })

  it("should have a driver object defined", () => {
    expect(driver).toBeDefined()
  })
})

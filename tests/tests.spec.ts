describe("environment", () => {
  it.skip("should have a browser object defined", () => {
    expect(browser).toBeDefined()
  })

  it.skip("should have a driver object defined", () => {
    expect(driver).toBeDefined()
  })
})
